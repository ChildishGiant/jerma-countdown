function sOrNah(e, t) {
    1 == e ? document.getElementById("time").innerHTML += "1 " + t + " " : e > 1 && (document.getElementById("time").innerHTML += e + " " + t + "s ");
}

var now = new Date(), testDay = new Date(), nextStream, dayNumber = now.getUTCDay(), dateNumber = now.getUTCDate();

window.onload = function() {
    function e() {
        var e = new Date(), m = t - e;
        if (m < 0) return clearInterval(timer), void (document.getElementById("time").innerHTML = "EXPIRED!");
        var i = Math.floor(m / o), d = Math.floor(m % o / r), l = Math.floor(m % r / a), u = Math.floor(m % a / n);
        document.getElementById("time").innerHTML = "", sOrNah(i, "day"), sOrNah(d, "hour"), 
        sOrNah(l, "min"), sOrNah(u, "second");
    }
    for (console.log("£sdftg"); ;) {
        if (2 == dayNumber || 4 == dayNumber || 5 == dayNumber) {
            var t = new Date(testDay.getFullYear(), testDay.getMonth(), dateNumber, 24);
            break;
        }
        testDay.setDate(testDay.getDate() + 1);
    }
    var n = 1e3, a = 60 * n, r = 60 * a, o = 24 * r, m = [ "Next magic show in", "The rumble will begin in" ];
    document.getElementById("splurge").innerHTML = m[Math.floor(Math.random() * m.length)], 
    e(), window.setInterval(function() {
        e();
    }, 1e3);
};